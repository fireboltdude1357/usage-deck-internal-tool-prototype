# archive/

Frozen artifacts. Read for history; don't update.

## archive/design/

The phase-by-phase design docs that drove the prototype build (phases 01–05,
all shipped). Includes the per-phase `PLAN.md` / `README.md` pairs and the
top-level `DESIGN.md` that supersedes the earlier SAM-stack architecture.

The four metric/data-source specs (`data-sources.md`,
`market-engagement-metrics.md`, `platform-engagement-metrics.md`,
`provisioned-users.md`) live here too — they were copied verbatim from
`internal-tool-try-1` and remain canonical *for the metric definitions*. If
you need to tweak a metric definition, copy the relevant section into
`docs/` rather than editing in place.

For the current architecture, see `docs/`.
